{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compose up",
            "type": "shell",
            "command": "docker-compose down && docker-compose --verbose -f docker-compose.debug.yml up --build --force-recreate  -d",
            "presentation": {
                "reveal": "never",
                "panel": "new"
            },
        },
        {
            "label": "compose down",
            "type": "shell",
            "command": "docker-compose down",
            "presentation": {
                "reveal": "never",
                "panel": "new"
            },
        },
        {
            "label": "compose up2",
            "type": "shell",
            "command": "docker-compose --verbose -f docker-compose.debug.yml up --build --force-recreate  -d",
            "presentation": {
                "reveal": "never",
                "panel": "new"
            },
        }
    ]
}